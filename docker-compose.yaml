version: "3.9"
services:
  client:
    build:
      context: ./client
      dockerfile: Dockerfile.dev
    container_name: client
    ports:
      - 3000:3000
      - 3100:3100
    volumes:
      - ./client:/app
    restart: always
    tty: true
    stdin_open: true
    command: "bash entrypoint.sh"